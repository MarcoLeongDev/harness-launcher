# DeepSeek Harness Launcher — example environment configuration
# Copy to .env (ignored by .gitignore) and fill with real values.

DSH_HOME=/Users/your-user/.dsh
LOG_MAX_BYTES=2097152

# Example engine token (not used; engine prints a per-process token at boot)
# ENGINE_TOKEN=replace-with-your-own
