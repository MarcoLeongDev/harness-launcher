{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability set for the harness webview window (localhost content) and the launcher Control Panel window (local dsh-ui protocol).",
  "windows": [
    "main",
    "settings"
  ],
  "remote": {
    "urls": [
      "http://127.0.0.1:*"
    ]
  },
  "permissions": [
    "core:default",
    "notification:default",
    "updater:default"
  ]
}
